// Temporary public entry-point closure. Set to true and redeploy to reopen.
// Existing sessions, session refresh, dashboard and admin authorization stay active.
export const PUBLIC_ACCESS_ENABLED = false;
